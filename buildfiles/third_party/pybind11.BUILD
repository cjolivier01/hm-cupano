# This BUILD file creates a package for the bazel directory inside pybind11.
package(default_visibility = ["//visibility:public"])

# Export the pybind11 Bazel rule file so that it can be loaded.
exports_files(["bazel/pybind11.bzl"])
